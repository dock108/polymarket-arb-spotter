# Data Directory

This directory is used for:
- SQLite database files (*.db)
- Log files (*.log)
- Cached data
- Export files (CSV, JSON)

**Note**: All files in this directory are gitignored except this README.
